# 🇨🇱 Chile & Easter Island Blog Generator

This tool automates the creation of a professional `.docx` travel blog. It reads trip data from a JSON file, automatically scales and inserts photos, and generates a formatted itinerary and table of contents.

## 🛠 Prerequisites

- [Node.js](https://nodejs.org/) (Version 16 or higher recommended)
- A folder named `photos/` containing your images.

## 🚀 Getting Started

1. **Install Dependencies**:
   Open your terminal in the project folder and run:
   ```bash
   npm install